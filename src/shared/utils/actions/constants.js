export const ALL_ENTRIES_PAGINATION = {
  page: 0,
  pageSize: 3000,
};

export const DEFAULT_CONFIG_SHAPE = {
  filters: {
    keepEmptyArrayFilter: [],
  },
};

export default {
  ALL_ENTRIES_PAGINATION,
  DEFAULT_CONFIG_SHAPE,
};
